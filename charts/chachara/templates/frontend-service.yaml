apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend-svc
spec:
  type: NodePort
  selector:
    app: chachara-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081